const GBDefenderAI = prov(() => {
  let u = extend(DefenderAI, { 
    findTarget(air,ground){
      let result = null;

      if(air){
        for(updateMovement){
          set(boost = true)
        }
      }  
      if(ground){
        for(updateMovement){
          set(boost = false)
        }
      }
    }
  }
  )
)    
}